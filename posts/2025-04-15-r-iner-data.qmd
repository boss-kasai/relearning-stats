---
title: 'Rに内蔵されたデータ'
author: "ThePastOfDice"
date: '2025-04-15'
categories: ["R"]
tags: ["R", "sample data"]
---

## R学習の際のサンプルデータ

大学を卒業してから定期的に「Rに触りたいな」と思うことはありましが、触る目的もデータもないので重い腰が上がらないまま月日が流れてしまいました。

改めてRを触ることに決めたので、サンプルデータをどうしようかと思ったところ、Rに一定のサンプルデータがあることを思い出しました。

そもそもどんなデータがあったのかを忘れてしまったので、どのようなデータがあったのか確認をしつつ今後の分析計画を決めたいと思います。

まずRに内包されているサンプルデータを確認してみます。

```{r}
data()
```

出力数が多いので、RStudioでは別タブで表示されると思います。
このデータはRに内包されているので、別でパッケージなどをインストールする必要はありません。

全データセットがかなりの数になるのでここで書くのは避けておきます。

この記事でどの分析に、どのデータセットがおすすめかをまとめたかったのですが、数が多く全てのデータを見ることができなかったので、今後私が触っていく中でこの記事に追記して内容を充実させていこうと思います。

ここでは最もよく使われるデータセットの１つである ```iris``` でデータの中身を確認します。

まずはirisにどのような列があるかを確認してみます。
```{r}
colnames(iris)
```

とりあえず、５つの列を確認できました。
英語なので、なんとなくどんな列かわかります。

列名だけではデータがよく分からないので、Rの標準関数であるhead()で先頭の５行のデータを見てみましょう。

```{r}
head(iris)
```

ちなみに、引数を指定することで、表示する行数を指定することもできます。

```{r}
head(iris, 10)     # 上から10行
head(iris, n = 3)  # 上から3行（引数を明示）
```

ちなみに、末尾からの取得も可能です。
```{r}
tail(iris)
```

実際にデータを見てみることも大切ですが、データの各項目(列)が意味すること路は調べた方が早いです。

ちなみにirisデータは下記のようになっています。
irisはアヤメという植物のことです。

| 列名           | 説明                             | データ型     | 単位         |
|----------------|----------------------------------|--------------|--------------|
| Sepal.Length   | がく片（Sepal）の長さ            | numeric（数値）| センチメートル |
| Sepal.Width    | がく片（Sepal）の幅              | numeric（数値）| センチメートル |
| Petal.Length   | 花弁（Petal）の長さ              | numeric（数値）| センチメートル |
| Petal.Width    | 花弁（Petal）の幅                | numeric（数値）| センチメートル |
| Species        | アヤメの品種（3種類のカテゴリ）  | factor（因子型）| -            |


今後、他の分析で適宜、組み込みデータを使うことがあります。
そんな時に「このデータ何！？」ってならないでもらえれば大丈夫です。
